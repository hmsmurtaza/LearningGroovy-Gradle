task groovy doLast{}

def doubleInt(n) {
    n + n // Note we don't need a return statement
}

def foo = 5
println "doubleInt($foo) = ${doubleInt(foo)}"

foo = "foobar"
println "doubleInt($foo) = ${doubleInt(foo)}"